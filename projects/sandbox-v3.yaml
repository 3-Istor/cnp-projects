apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sandbox-v3-bootstrap
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/3-Istor/cnp-project-base.git
    targetRevision: HEAD
    path: .
    helm:
      values: |
        projectName: "sandbox-v3"
        features:
          gatus: true
  destination:
    server: https://kubernetes.default.svc
    namespace: sandbox-v3-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
