apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: le-deux-bootstrap
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/3-Istor/cnp-project-base.git
    targetRevision: HEAD
    path: .
    helm:
      values: |
        projectName: "le-deux"
        features:
          gatus: true
  destination:
    server: https://kubernetes.default.svc
    namespace: le-deux-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
